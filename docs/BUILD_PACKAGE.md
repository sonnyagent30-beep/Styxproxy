# MOVED → archive/BUILD_PACKAGE.md

This file has been moved to `archive/BUILD_PACKAGE.md`.

The architecture it describes (Google Sheets + Ollama) has been replaced by PostgreSQL + MiniMax M2 + Redis.

See the current system in `docs/ARCHITECTURE_PLAN.md` and `docs/DEPLOYMENT.md`.
