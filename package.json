{
  "name": "styxproxy",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "next": "15.3.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4"
  },
  "scripts": {
    "build": "cd frontend && npm install --include=dev && next build && mv .next ../.next"
  }
}
