{
  "name": "styxproxy",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "next": "15.5.23"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4"
  },
  "scripts": {
    "build": "cd frontend && npm install && next build && mv .next ../.next"
  }
}
