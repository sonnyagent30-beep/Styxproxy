{
  "name": "styxproxy",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "next": "latest"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "playwright": "^1.62.1"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  }
}
