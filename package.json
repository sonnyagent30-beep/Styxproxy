{
  "name": "styxproxy",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@sentry/nextjs": "10.68.0",
    "@tailwindcss/postcss": "^4",
    "playwright": "^1.62.1"
  },
  "scripts": {
    "build": "echo 'Vercel handles frontend build via rootDirectory'"
  }
}
