{
  "name": "styxproxy",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@sentry/nextjs": "10.68.0",
    "next": "16.2.10"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
