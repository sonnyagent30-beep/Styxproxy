name: Super Simple Secret Test
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "hello"
          echo "Length: ${#ADD_KEY_API_KEY}"
        env:
          ADD_KEY_API_KEY: *** secrets.ADD_KEY_API_KEY }}
